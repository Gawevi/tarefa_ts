function multiplica(numero1: number, numero2: number): number {
    return numero1 * numero2;
}

console.log(multiplica(10, 10));

//////

function dizOi(nome: string): string {
    return `Olá ${nome}`;
}

console.log(dizOi("Gabriel"));